specVersion: "v1"
serviceId: "soil-farming-agent"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "soil-farming-agent-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
